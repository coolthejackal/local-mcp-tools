# Bağımlılıklar
ContextMcp/node_modules/
ContextMcp/dist/

# Ortam ve gizli bilgiler
.env
.env.*
!.env.example

# LLM enrichment cache (CTX_ROOT içinde de yazılabilir; repo köküne yazılırsa da dışla)
.cache/mcp-llm/

# Otomatik üretilen manifest (build_context çalıştırınca yenilenir)
mcp-index.json

# Kullanıcıya özel MCP yapılandırması (.mcp.json.template olarak örnek sağlanıyor)
.mcp.json

# Claude Code ve editör ayarları (kullanıcıya özel)
.claude/
.vscode/

# Derleme çıktısı
*.js.map

# .NET (Roslyn manifest aracı) derleme çıktısı
ContextMcp.Roslyn/bin/
ContextMcp.Roslyn/obj/
*.lscache
